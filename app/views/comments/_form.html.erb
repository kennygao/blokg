<%= form_for [@post, @comment] do |f| %>
	<% if @comment.errors.any? %>
		you got <%= pluralize(@comment.errors.count, 'issue') %>
		<ul>
			<% @comment.errors.full_messages.each do |m| %>
				<li><%= m %></li>
			<% end %>
		</ul>
	<% end %>
	
	<p>
		<%= f.label :commenter %><br>
		<%= f.text_field :commenter %><br>
	</p>

	<p>
		<%= f.label :text %><br>
		<%= f.text_area :text %><br>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>
